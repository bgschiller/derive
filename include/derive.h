namespace Derive {
/**
 * Create a toString() method that pretty-prints
 * all member variables. Each member must be either
 * - a primitive type
 * - a type implementing toString()
 * - a stl container of one of the above.
 */
struct show {};
} // namespace Derive
